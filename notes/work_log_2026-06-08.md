# Work Log 2026-06-08

## 一、Free 路径 StackWriter 对齐

在 `RecordFreeThreadLocal` 中加了 StackWriter 路由，free 路径也走 `RecordStackWriterSubAblationFreeThreadLocal`。
与 alloc 路径对称——同样的 Write → (可选 batch) → Flush 管线。

**效果**：性能中性（误差范围内），价值在于数据质量——sub=34/35/36 现在测量统一的 StackWriter 路径，
不再混入 HookWriter 默认逻辑。

Commit: `5ab84a2`

## 二、可配置锁延迟（模拟更长临界区）

加 `LNHV1_LOCK_DELAY_NS` 环境变量，在 `StackWriter::WriteLocked()` 的 memcpy 后注入 busy-wait，
模拟真实 OH 的 `ShareMemoryBlock::PutWithPayloadTimeout` 更重的临界区。

**Sub=34 延迟 sweep 结果**：

| delay | 1T | 16T | 16T/1T |
|---|---|---|---|
| 0ns | 0.233s | 0.685s | 2.9x |
| 100ns | 0.229s | 0.648s | 2.8x |
| 500ns | 0.251s | 0.714s | 2.8x |
| 1000ns | 0.286s | 0.758s | 2.6x |

即使临界区拉长 40 倍，16T/1T 竞争比没有显著恶化。inner_mutex_ 不是扩展瓶颈。

Commit: `86edf0d`

## 三、Consumer Profiling

Consumer 加 `--profile` 标志，输出每次 wakeup 的耗时拆解。

**Sub=36 1T 100K iters 数据**（4993 次 wakeup 平均）：

| 阶段 | 耗时 | 占比 |
|---|---|---|
| eventfd read | 3961ns | 66% |
| ring drain (ConsumeAvailable) | 423ns | 7% |
| printf output | 1610ns | 27% |

Consumer drain 几乎免费（423ns 处理 ~20 条记录）。生产模式去掉 printf 后，consumer 每 wakeup ~4.4μs。

Commit: `1c00982`

## 四、Flush Threshold Sweep

Sub=36 全链下 sweep flush_threshold 1~200：

| ft | 1T | 4T | 8T | 16T |
|---|---|---|---|---|
| 1 | 1.058s | 0.966s | 1.125s | 1.093s |
| 10 | 0.367s | 0.877s | 1.072s | 1.032s |
| 20 (OH 默认) | 0.318s | 0.929s | 0.941s | 1.033s |
| 50 | 0.282s | 0.426s | 0.982s | 1.046s |
| 100 | 0.259s | 0.730s | 0.997s | 1.025s |
| 200 | 0.244s | 0.451s | 0.957s | 0.975s |

关键发现：
- ft=1→10 是最大跳跃（1T: 2.9x），之后边际递减
- OH `FLUSH_FLAG=20` 是合理的默认值
- 提至 50 可再省 ~12%（1T），对多线程无负面影响

## 五、OH Fork 翻译检查

检查 `cyc_nativehook` 真实代码后发现：
- Eventfd 阈值：`FLUSH_FLAG=20`, `PrepareFlush()` 已有 — 不需要改
- Free 走 StackWriter：`hook_free` → `SendStackWithPayload` — 已有

原型最近的"优化"是在追赶真实代码已有的设计。当前无可新增移植的优化。

## 六、Commit 链（6/8 新增）

- `5ab84a2` Route free path through StackWriter for sub-stage 34/35/36
- `3954136` Update AGENTS.md: commit stack, ablation status, remaining directions
- `e6566ae` Deduplicate hot-path mapping table in AGENTS.md
- `86edf0d` Add configurable lock delay to simulate longer critical section
- `1c00982` Add --profile flag to consumer for timing breakdown
